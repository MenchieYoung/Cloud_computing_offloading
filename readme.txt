mulphr为主函数，运行mulphr即可；


output输出三种算法的三种目标函数值；


稍微有问题的一个地方：
m，D_sum等参数有时候改动较大，会导致结果不好...(论文里表格有提到)
目前这种参数设置下，m最好在4到9之间取值
(m小于等于3时收敛不到可行域，m大于9时设置的迭代次数好像不够)



三种算法结果比较图的程序（draw.m）比较简单，就每次记录下数据，另外单独画的图。